{
  "name": "mindtrack-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "start": "npm start --prefix backend"
  }
}
